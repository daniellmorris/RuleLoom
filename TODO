I want to make it so a plugin closure that the closure can be a config that uses other closures. Plugins should be able to use plugins too. If a plugin is using a plugin then the main runner should load dependencies prior to loading the main plugin.


